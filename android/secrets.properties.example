# Prefer the project-root `.env` file instead.
# This file is only a fallback if you keep Android-only secrets here.
# Copy to secrets.properties (gitignored) if needed.

GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE
